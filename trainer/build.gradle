apply plugin : 'java'
apply plugin : 'application'

version = '1.0.0'

dependencies {
    compile project(':core')
}

mainClassName = 'Trainer'

run {
    if (project.hasProperty("appArgs")) {
        args Eval.me(appArgs)
    }
}